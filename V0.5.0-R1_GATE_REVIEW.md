# 食策AI V0.5.0-R1 — Data Integrity Gate Review

**Review date:** 2026-09-01  
**Scope:** Data Foundation integrity fix only  
**Gate result:** **PASS（R1）**

> R1 只修复 V0.5.0 复审确认的数据完整性、输入兼容、备份/恢复与最小版本信息问题。没有进入 V0.5.1，没有做 UI 大改、AI 重构、税务、BOM、库存、平台 API/RPA。

## 1. 为什么需要 R1

V0.5.0 首轮虽然已有 42 个测试通过，但复审发现正常路径测试不足以覆盖真实门店数据风险。重点问题包括：

1. 缺字段修正版会把已有字段写成 NULL。
2. Settlement 修正版可能删除已有平台费用明细。
3. 周期结算费用被错误归到周期最后一天。
4. API 指定渠道与文件渠道冲突时会产生批次/数据渠道不一致。
5. 手工 reconciliation 可跨渠道匹配。
6. 空 CSV / 坏 JSON 可出现 0 行但 `COMPLETED`，且解析失败前无 Import Batch。
7. GB18030 与非补零日期兼容不足。
8. `stores.type` 与 `stores.store_type` 可能分叉。
9. 固定 V0.4 备份文件可能是旧备份而非本次升级前数据库。
10. `channel_net_revenue` 与 `expected_settlement` 混为同一计算。
11. 现有“导出备份/清空全部数据”只覆盖 legacy state，不覆盖 Schema V2。
12. 正式包前端仍显示 V0.4 与底部演示门店卡。

## 2. R1 采用的关键策略

### 2.1 缺字段修正版：安全拒绝，不做自动 merge

最初复审建议考虑 Partial Update，但进一步检查发现：如果自动把新文件提供的字段与旧文件保留字段合并，一条 normalized record 的不同字段会来自不同 Import Batch，而当前 Schema V2 没有字段级 provenance 表。

因此 R1 选择更保守且可审计的策略：

- 已有记录包含非空字段；
- 后续同逻辑记录缺少这些已有字段；
- 系统拒绝本次更新；
- 原记录完全保留；
- Import Batch / Raw Row 记录失败原因；
- 用户需上传完整修正版。

这样避免数据被清空，也避免制造“混合来源记录”。

### 2.2 结算周期费用不伪造日归属

`settlements` 拆出的 `platform_fees.business_date` 默认保持 `NULL`。

例如 8/1–8/7 一周佣金 ¥700，不再自动记到 8/7。

### 2.3 渠道冲突 fail closed

如果本次导入指定 `MEITUAN_DELIVERY`，但文件行写 `DOUYIN_LOCAL`：

- Raw Row 保留；
- 行状态 `REJECTED`；
- Import Batch 为 `FAILED`/`PARTIAL`；
- normalized 数据不落库。

Reconciliation 手工匹配同时检查 `store_id` 与 `channel_id`。

## 3. 修改文件

核心生产代码：

- `data_foundation.py`
- `db_v2.py`
- `app.py`
- `static/index.html`
- `启动_食策AI_Windows.bat`

测试：

- 新增 `tests/test_data_integrity_r1.py`

文档：

- `README.md`
- `使用说明.txt`
- `docs/superpowers/plans/2026-09-01-v050-r1-data-integrity.md`
- 本 Gate Review

原 V0.5.0 Gate/Verification 已移至：

- `docs/history/V0.5.0_GATE_REVIEW_PRE_R1.md`
- `docs/history/V0.5.0_FINAL_VERIFICATION_PRE_R1.txt`

## 4. 导入体系修复

### 4.1 Parse 失败也创建 Import Batch

当前流程：

`File → import_files → import_batches(PROCESSING) → Parse → Raw Rows / FAILED report`

以下情况均产生可审计失败批次：

- 空 CSV
- malformed JSON
- 无可识别行
- 行级字段错误

0 行不再标记 `COMPLETED`。

### 4.2 编码兼容

V2 parser 和 legacy server upload 均支持：

- UTF-8 BOM
- UTF-8
- GB18030

### 4.3 日期兼容

例如：

- `2026/8/7`
- `2026-8-7`

统一保存：

- `2026-08-07`

## 5. Migration 与备份

真实 V0.4 数据库副本验证：

- Before schema: 1
- After schema: 2
- `app_state` 保留：YES
- store 保留：YES
- 9 个标准 channels：YES
- canonical V0.4 backup 与升级前数据库 SHA-256 完全一致：YES
- 同时创建 timestamped backup：YES

备份策略：

- 真正从 V0.4 → V2 时，`*_v04_backup.db` 刷新为本次升级前数据库；
- 同时额外创建带时间戳备份；
- 已是 Schema V2 时不会重复创建 V0.4 升级备份。

## 6. Backup / Reset / Restore

### Server mode

`GET /api/backup`

返回 `shice-ai-full-v1`：

- legacy state
- 可检查的 Schema V2 数据摘要
- 完整 SQLite snapshot（base64）
- SHA-256

`POST /api/reset`

清除当前门店：

- daily_channel_sales
- settlements
- platform_fees
- payments
- reconciliation_matches
- raw_import_rows
- import_batches
- import_files
- legacy uploads/state 数据

保留：

- schema
- channels
- users / store 基础结构

`POST /api/restore`

- 校验 backup format
- 校验 base64
- 校验 SHA-256
- 校验 SQLite header/schema
- staged restore
- 替换前创建 `pre_restore` safety backup
- atomic replace

测试已验证：

`有销售数据 → backup → reset → 0 条销售 → restore → 销售恢复`

## 7. Money / Calculation 语义

财务核心继续使用 `Decimal`。

`calculate_expected_settlement()`：结算口径。

`calculate_channel_net_revenue()`：已分离为独立 revenue profile，不再直接调用 expected settlement。

R1 不自动选择平台专用 profile，不猜测平台补贴/费用口径。

## 8. Store Compatibility

保存设置时同步：

- `stores.type`
- `stores.store_type`

避免 V0.4 页面与 Schema V2 服务读取不同门店类型。

## 9. Minimal Front-end Correction

未重构 UI。

只做：

- 显示版本更新为 `V0.5.0-R1`
- 删除左侧底部“永民手作 / 奶茶店 · 本地演示数据”卡片
- `renderAll()` 对该 DOM 改为可选，避免 null error
- Server mode 导出完整 backup
- Server mode 恢复完整 backup
- “清空全部数据”真正调用后端清理 legacy + V2
- localStorage fallback key `shice_v04_state` 保留，作为兼容契约，不改名

## 10. Automated Tests

最终完整测试：

**58 passed / 0 failed / 2 deprecation warnings**

其中 R1 新增 16 个针对真实风险的测试，覆盖：

- incomplete sales correction fail-safe
- incomplete settlement correction fail-safe
- settlement fee business_date not fabricated
- explicit channel conflict
- cross-channel reconciliation rejection
- empty CSV failed batch
- malformed JSON failed batch
- GB18030 + unpadded date
- stale backup refresh
- channel revenue / settlement separation
- type/store_type sync
- full backup/reset/restore
- empty V2 API upload 400 + failed batch
- malformed JSON API 400 + failed batch
- legacy GB18030 upload
- static version / bottom card regression

旧 V0.4/V0.5.0 回归测试全部继续通过。

## 11. Build / Syntax / API Smoke

- `python -m compileall`：PASS
- Extracted front-end JS `node --check`：PASS
- FastAPI actual server smoke：PASS
  - `/api/health` → 200
  - `/api/state` → 200
  - `/api/v2/channels` → 200
  - `/api/backup` → 200
- channels count：9
- backup format：`shice-ai-full-v1`

## 12. 浏览器验证限制

当前执行环境的 Chromium 管理策略会阻止访问 `127.0.0.1` 和 `file://`，因此本轮无法用 Playwright 做真实整页浏览器截图验收。

没有把这一项伪报为 PASS。

已完成替代验证：

- 前端 JS syntax check
- 前端静态契约测试
- V0.4 fallback regression test
- FastAPI 实际进程 + curl smoke test

用户在本机启动后仍建议做一次最终人工浏览器目检。

## 13. Known Limitations

1. **缺字段 correction 当前直接拒绝。** 尚未实现字段级 provenance merge；这是刻意的安全边界。
2. **无 bank_reference 的到账更正**只能依赖 composite fingerprint；没有稳定外部引用时无法百分百识别“同一笔更正”。
3. **无 settlement_reference 的结算更正**需要稳定周期/日期组合才能识别同一逻辑记录。
4. Calculation Profile 仍为显式选择；尚未开发美团/抖音/京东平台专用 parser/profile。
5. Full JSON backup 内嵌 SQLite snapshot，适合当前单店原型；大规模 SaaS 以后应改为正式备份存储机制。
6. FastAPI `on_event` 仍有 2 条 deprecation warning，沿用旧架构，本 R1 未顺手重构。
7. 前端仍是 legacy state 页面，本轮只做必要一致性修正。

## 14. Gate Checklist

- [x] V0.4 DB 可原地升级
- [x] 升级前备份是本次真实快照
- [x] Migration rollback/idempotency 保持
- [x] 重复销售/结算/到账不重复累计
- [x] 缺字段更正不清空已有数据
- [x] 不产生混合来源 partial merge
- [x] 空/坏文件有 FAILED Import Batch
- [x] 渠道冲突拒绝
- [x] 跨渠道 reconciliation 拒绝
- [x] 周期费用不伪造日归属
- [x] Revenue / Settlement 语义分离
- [x] Backup/Reset/Restore 覆盖 V2
- [x] GB18030 / 常见日期兼容
- [x] type/store_type 同步
- [x] V0.4 API/fallback 回归通过
- [x] 无 UI 大改
- [x] 未进入 V0.5.1
- [x] 自动测试全绿

# Final Gate

**V0.5.0-R1 Data Integrity Gate = PASS**

下一步如继续，只建议先由用户做本机浏览器目检；随后再单独定义 V0.5.1，不自动继续。
