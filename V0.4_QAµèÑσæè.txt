食策AI V0.4 API QA

PASS 1/9  服务健康检查与版本
PASS 2/9  演示账户 / 门店会话
PASS 3/9  状态重置为空状态
PASS 4/9  服务端一次载入4类示例数据
PASS 5/9  服务端问答并持久化
PASS 6/9  服务端营销生成并持久化
PASS 7/9  服务端CSV上传与解析
PASS 8/9  上传审计记录
PASS 9/9  状态PUT写入SQLite

额外验证：服务重启后，SQLite中的门店状态、问答历史、营销历史仍可恢复（持久化通过）。
打包前数据库已重置为空状态，首次打开不会凭空显示演示结果。

前端JS：Node syntax check PASS。
后端Python：py_compile PASS。
说明：当前环境的Chromium策略阻止访问127.0.0.1，因此本轮无法用浏览器自动化直接访问本地服务做整页截图；V0.4前端沿用已通过多档响应式检查的V0.3 UI，并仅加入服务端同步、状态标签与设置页服务信息。
